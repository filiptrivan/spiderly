@Component({
    selector: 'brand-fields',
    template: `
<ng-container *transloco="let t">
    <ng-content select="[before]"></ng-content>
    <div *ngIf="config.showName !== false" class="col-8 md:col-4">
        <spiderly-textbox [control]="formGroup.controls.brandDTO.getControl('name')"></spiderly-textbox>
        <ng-content select="[belowName]"></ng-content>
    </div>
    <div *ngIf="config.showStatus !== false" class="col-8 md:col-4">
        <spiderly-dropdown [control]="formGroup.controls.brandDTO.getControl('status')" [options]="statusOptions" (onChange)="onStatusChange.next($event)"></spiderly-dropdown>
        <ng-content select="[belowStatus]"></ng-content>
    </div>
    <ng-content select="[after]"></ng-content>
</ng-container>
    `,
    imports: [
        CommonModule,
        FormsModule,
        ReactiveFormsModule,
        SpiderlyControlsModule,
        TranslocoDirective,
    ]
})
export class BrandFieldsComponent {
    @Input() formGroup: SpiderlyFormGroup<BrandSaveBody>;
    @Input() config: BrandFieldsConfig = {};
    @Output() onStatusChange = new EventEmitter<DropdownChangeEvent>();
    @Input() statusOptions: Namebook[];
}

export class BrandFieldsConfig {
    showName?: boolean;
    showStatus?: boolean;
}